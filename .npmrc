# pnpm 10 default: dependencies are not hoisted to the root (avoids phantom packages).
# Keep this explicit so consumers know the layout.
shamefully-hoist=false

# Set to false so peer-dep warnings don't break install; run `pnpm ls --depth=0`
# periodically to audit any missing peers.
strict-peer-dependencies=false